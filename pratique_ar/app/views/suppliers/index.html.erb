<p id="notice"><%= notice %></p>

<h1>Suppliers</h1>

<table>
  <thead>
    <tr>
      <th>Supplier name</th>
      <th>Contact name</th>
      <th>Address</th>
      <th>City</th>
      <th>Postal code</th>
      <th>Country</th>
      <th>Phone</th>
      <th>Products</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @suppliers.each do |supplier| %>
      <tr>
        <td><%= supplier.supplier_name %></td>
        <td><%= supplier.contact_name %></td>
        <td><%= supplier.address %></td>
        <% if supplier.city.present? %>
          <td><%= link_to supplier.city.name, city_path(supplier.city_id) %></td>
        <% else %>
          <td>n.a.</td>
        <% end %>
        <td><%= supplier.postal_code %></td>
        <td><%= link_to supplier.country.name, country_path(supplier.country_id) %></td>
        <td><%= supplier.phone %></td>
        <td><%= link_to "Products", products_path(field_name: :supplier_id, value: supplier.id) %></td>
        <td><%= link_to 'Show', supplier %></td>
        <td><%= link_to 'Edit', edit_supplier_path(supplier) %></td>
        <td><%= link_to 'Destroy', supplier, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Supplier', new_supplier_path %>
